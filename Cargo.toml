[package]
name = "magic-eye-games"
version = "0.1.0"
edition = "2021"

[dependencies]
bevy = "0.18"
rand = { version = "0.8", features = ["small_rng"] }

# Speed up iteration builds
[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3
